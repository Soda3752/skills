# Codex pane 派工指令模板

每一段都對應一個實際踩過的坑。**照結構組，但內容要換成這個專案、這張票的真實情況**——模板裡的具體檔名與理由是範例，照抄過去會變成謊話。

## 目錄

- [為什麼是散文而不是 XML](#為什麼是散文而不是-xml)
- [完整模板](#完整模板)
- [逐段說明](#逐段說明)
- [補正指令怎麼寫](#補正指令怎麼寫)

---

## 為什麼是散文而不是 XML

`codex-wave` 用 `codex exec` 的一次性背景 job，那種場景下 XML 區塊有幫助（機器讀、無互動、要求結構化）。

**互動 pane 不一樣**：Codex 在 pane 裡是連續對話，會反覆讀你這則訊息當作整輪的憲章。實測散文加標題效果好，因為理由講得出來——而理由是它自己判斷邊界情境時唯一能依據的東西。**指令裡每一條限制都附上「為什麼」**，它遇到模板沒寫到的情況時才知道往哪邊靠。

---

## 完整模板

```markdown
你在 git worktree <worktree 絕對路徑> 裡，分支 <branch>，從 <base>@<hash> 建立。
請把 Linear 票 <TICKET> 做到可以合併為止。

## 第一步：自己讀票
用 Linear MCP 執行 get_issue({ id: "<TICKET>", includeRelations: true }) 讀完整描述與驗收條件。
**這張票上有一則我剛寫的「已解鎖」註解，用 list_comments({ issueId: "<TICKET>" }) 一併讀** ——
裡面寫了現在可以直接用什麼、以及該避開的坑。票與註解是唯一權威，
不要靠我這則訊息裡的摘要做事。

## 你可以讀但絕對不能寫的地方
主 repo <主 repo 絕對路徑> 只能讀（規格文件 <實際路徑> 在那裡）。
任何寫入動作只能發生在你自己的 worktree 內。

## 上游已經做好的東西，直接用，不要重寫
<具體匯出／元件／路由，附檔案路徑>
<已由整合時實測確認的事實，例如「66 家店只要 15 ms，可以放心做成同步呼叫」>
<可以照抄的既有實作：「X 已經做過一次完整流程，包含 A、B、C。照著做，不要重新發明。」>

## 照既有模式做，不要另發明一套
專案已經有 <同類實作> 走完全相同的路徑（<票號>建立），
**先把這幾個檔讀完再動手，然後鏡像它**：
- <檔案 1> — <裡面有什麼、檔頭註解交代了什麼設計理由>
- <檔案 2> — <同上>
這些理由對你這張票同樣成立，照做。

## 這一波還有誰在跑
同時有另一個 Codex 在做 <TICKET-B>（<標題>），它的戰場是 <檔案清單>。
**這些檔你一個都不要碰。**
<獨佔檔宣告：「package.json 這一波由你獨佔」或「你不要碰 package.json」>

**唯一的共用檔是 <路徑>，兩邊都會往檔尾追加。衝突紀律：**
- 新內容一律加在**檔案最末端**，並用註解標出區塊（例如 `/* <TICKET> <用途> */`）
- **只改最小必要行數，絕對不要順手重排、合併或重構這個檔的既有內容**
- 不要動 <前一張票> 在尾段加的那批內容

## 環境已經幫你備好，不要重做
node_modules、<gitignore 的設定檔清單> 都已複製完成。**不要跑 npm install / npm ci。**

## 驗證閘門（只跑這 N 條）
<逐條列出當下實際會綠的指令>

**刻意排除的 M 條與理由（不要去修，那不是你的票）：**
- <指令> — <真因，例如「輸入檔 X 在 base 分支上就已經不存在，動工前就是紅的」>
- <指令> — <真因，例如「需要 dev server 在跑，沒起 server 就是 404，動工前就是紅的」>

<若某條閘門對這張票特別重要就點名>
**`<指令>` 對你特別重要**（目前 N 條斷言），它正是涵蓋 <範圍> 的那一支。
若你的改動讓它的斷言數變少或行為改變，那是回歸，要停下來查清楚。

建置輸出動輒數千行：一律重導到檔案再看結尾，例如
  npm run build > /tmp/<ticket>-build.log 2>&1; tail -30 /tmp/<ticket>-build.log
不要讓完整輸出直接進你的 context。

## 補自動化驗證
如果你的邏輯有任何可以用純函式驗證的部分，優先加進既有的 <腳本> 或新增一支 `verify:*` 腳本
—— 這是這個專案唯一有效的自動守門，比在 RESULT.md 裡宣稱「應該沒問題」有價值得多。

## 你自己要做的對抗式自審
程式寫完、閘門全綠之後，回頭讀一次自己寫的碼，特別檢查：
- <這類型的票最容易出錯的 3~4 點，具體到這張票>

## 誠實度要求（這一點最重要）
<專案的實際守門缺口，例如「本專案 E2E 已關閉，瀏覽器層完全沒有任何自動守門」>
`typecheck` / `lint` / `build` 全綠**完全不代表 <實際要驗的東西>**。
你沒有 <實際驗證動作> 就必須標成「未驗證」。
把沒驗的寫成通過，不會有任何機器擋得住，但會讓後面接手 <下游票號> 的人踩空。

## 完成後要做的事
1. 在 <branch> 上 commit（訊息用繁體中文，conventional commit 格式）。
2. **不要 push、不要 merge、不要碰 <base>、不要碰別的分支、不要動任何其他票。**
3. **不要改動 Linear 的票券狀態、不要動 labels、不要碰其他票** —— 那是主控 Agent 的職責。
   但**這張票的實作紀錄註解由你自己寫**：`save_comment({ issueId: "<TICKET>", body: ... })`，
   標題 `## 實作紀錄（codex pane · <TICKET>）`，格式照
   `<主 repo>/.claude/linear-workflow.md` 的「註解怎麼寫」。結論在前、證據往摺疊區沉，
   並且要有這三段：**我考慮過但沒選的做法**（方案 → 一句理由）、
   **我撞到的牆**（試了什麼 → 原始錯誤訊息 → 改用什麼）、**我沒驗到的**（哪一條 → 具體障礙）。
   worktree 會被移除、RESULT.md 會跟著消失，這則註解才是永久紀錄。
4. 把結果寫成 RESULT.md 放在 worktree 根目錄（**不要 commit**），繁體中文，包含：
   - commit hash
   - 改了哪些檔案、為什麼
   - **驗收條件逐條對照**：每條寫「通過／部分通過／未驗證」，並附上你實際跑的驗證證據
   - 與票券描述不同的決策及理由
   - 票上沒列但你遇到並處理掉的邊界情境
   - **已知限制與未驗證部分**
   - <若有部署或營運步驟：正式環境要做什麼>

## 禁止修改的檔案
<專案的 protectedPaths> + <同波其他票的戰場>

做完就停下來，我會來收。
```

---

## 逐段說明

**「自己讀票」＋「一併讀解鎖註解」** —— 讀票省你的 context，而票是唯一權威。**讀解鎖註解是分波推進的複利所在**：上一波你審碼發現的邊界、效能特性、可以照抄的實作，全都寫在那裡。實測有效——第二波的 pane 因此不必自己重新發現「回傳的是原物件本身」「`optimizedCount === 0` 的語意」「`NaN` 不被當缺座標」這三件事，而且它真的照著把 `NaN` 正規化掉了。

**主 repo 唯讀** —— 規格文件常在 gitignore 的目錄裡（vault、docs、報告），只能從主 repo 讀。但絕不能在那裡寫，否則兩個 pane 會互相踩。

**「上游已經做好的東西」** —— Codex 不知道上一張票做了什麼。不寫的話它會重新實作一份，而那份與已經進 base 的那份會有微妙差異。**特別要寫「已由整合時實測確認的事實」**，那些是你補測出來、票面沒有的資訊。

**「照既有模式做」＋指路到具體檔案** —— 這是實測中效果最明顯的一段。Codex 沒讀過你的架構決策，但**它很擅長照著讀懂的模式做**。指路到有詳盡檔頭註解的既有實作，比寫任何抽象規範都有效——實測結果是它完整鏡像了模組層級 promise 快取、失敗時清快取、刻意沒有 loading 狀態這三個設計決策，並在 RESULT.md 裡解釋了為什麼。

**同波鄰居與獨佔檔** —— pane 知道有鄰居才會自我克制。獨佔宣告要正反都寫（對 A 說「你獨佔」、對 B 說「你不要碰」），只寫一邊會有人越界。

**共用檔的衝突紀律** —— 中風險共用檔（append-only 清單、樣式表尾段、路由表）仍可同波，但要下紀律。**「不要順手重排」這句必須明寫**，否則整個檔會變成一團無法 review 的 diff。

**環境已備好、不要 npm install** —— 不寫的話它會花好幾分鐘重裝一份已經在那裡的 `node_modules`。

**閘門清單 + 排除理由** —— **這是本工作流最重要的一段。** 只列當下實際會綠的；排除的要**寫出真因**並明令不要去修。不寫理由的話它會以為那是自己弄壞的，然後花整輪去修一個與這張票無關的既有問題。

**點名關鍵閘門與當前斷言數** —— 給它一個具體的回歸判準。實測有效：pane 把斷言數從 31 加到 35 並在 RESULT.md 裡主動報告「原 31 條未減少」。

**建置輸出重導** —— 數千行的建置輸出直接進 context 會癱瘓 pane。

**補自動化驗證** —— 自動守門薄的專案值得專門要求一段。實測有效，而且這是唯一能跨波累積的資產。

**對抗式自審** —— 要具體到這張票的類型。「純演算法票」要檢查收斂條件與邊界節點數；「拆頁票」要檢查兩條路由的行為有沒有悄悄分岔；「地圖票」要檢查序號對照與 effect 依賴。抽象地說「請自我 review」沒有用。

**誠實度要求** —— **不要只說「請誠實」。** 要指出這個專案的守門具體缺在哪、`build` 綠燈的邊界在哪、以及**寫錯會害到誰**（下游票號）。實測中最好的一次結果是 pane 把驗收條件 1 直接標成「未驗證」而不是含糊的「部分通過」——那正是這段起了作用。

**不准 push / merge / 動 base / 動 Linear** —— 整合與看板正確性是主控 Agent 的職責。即使 Codex 有 Linear MCP 也要明令，否則看板上的因果關係會變得無法追溯。

**RESULT.md 不要 commit** —— 不寫的話它會 commit 進去，然後每條分支都在同一個檔案上衝突。

---

## 補正指令怎麼寫

pane 沒達標時**不要整段重述**，用同一個 pane 送只有差異的指令——它保留了讀過的票與跑過的建置：

```markdown
<具體問題>。<為什麼這是問題>。

請只改這一點，其餘不動，然後重跑 <相關閘門> 並更新 RESULT.md 的對應條目。
```

**同一張票補正兩次仍不達標就停下來問使用者。** 多半是票券規格本身有歧義，那是人要決定的事，繼續補正只會累積更難 review 的 diff。
